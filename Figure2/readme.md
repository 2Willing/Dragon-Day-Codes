The codes in this directory is used to generate the intensity and base-difference maps in Figure 2.

If the parameter 'rd' in the code is set to 1, then it will produce base-difference maps, otherwise it will produce intensity maps.

